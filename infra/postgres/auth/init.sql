-- ============================================================
-- Инициализация БД auth-service (lorelounge_auth)
-- Запускается автоматически при первом старте postgres_auth.
-- Основные таблицы создаются через Alembic миграции — не тут.
-- ============================================================

-- Расширения
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";  -- для UUID-генерации
CREATE EXTENSION IF NOT EXISTS "pg_trgm";    -- для быстрого поиска по login/email (ILIKE)

-- Индекс для pg_trgm будет создан Alembic'ом после миграций
